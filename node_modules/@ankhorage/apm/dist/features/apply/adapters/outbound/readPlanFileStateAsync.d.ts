import type { ApmPlanDigestPort } from '../../../../types/plan.js';
/*** Read one project-relative text file and compute the canonical plan digest when it exists. */
export declare function readPlanFileStateAsync(rootPath: string, relativePath: string, digest: ApmPlanDigestPort): Promise<PlanFileState>;
interface PlanFileState {
    readonly exists: boolean;
    readonly content?: string;
    readonly digest?: string;
}
export {};
//# sourceMappingURL=readPlanFileStateAsync.d.ts.map