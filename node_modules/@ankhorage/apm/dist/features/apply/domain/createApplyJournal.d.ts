import type { ApmApplyJournal, ApmApplyPermissions } from '../../../types/apply.js';
import type { ApmPlanResult } from '../../../types/plan.js';
/*** Create the durable initial journal for one reviewed plan before any project side effect. */
export declare function createApplyJournal(plan: ApmPlanResult, permissions: ApmApplyPermissions, operationId: string, now: string): ApmApplyJournal;
//# sourceMappingURL=createApplyJournal.d.ts.map