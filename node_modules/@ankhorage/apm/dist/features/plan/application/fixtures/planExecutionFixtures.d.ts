import type { ApmPlanStepExecution } from '../../../../types/plan-execution.js';
import type { ApmMigrationDescriptor } from '../../../../types/update-protocol.js';
/*** Build reusable executable migration/projection evidence for plan behavior fixtures. */
export declare const planExecutionFixtures: {
    readonly migration: (descriptor: ApmMigrationDescriptor) => Extract<ApmPlanStepExecution, {
        readonly kind: "migration";
    }>;
    readonly projection: (input: ProjectionFixtureInput) => Extract<ApmPlanStepExecution, {
        readonly kind: "projection";
    }>;
};
interface ProjectionFixtureInput {
    readonly projectionId: string;
    readonly path: string;
    readonly content: string;
    readonly beforeDigest: string;
    readonly afterDigest: string;
    readonly generatorFingerprint: string;
    readonly version?: string;
}
export {};
//# sourceMappingURL=planExecutionFixtures.d.ts.map