import type { ApmPlanResult } from '../../../types/plan.js';
/*** Serialize immutable plan identity while keeping registry freshness outside equivalence semantics. */
export declare function buildPlanIdSource(plan: Omit<ApmPlanResult, 'schemaVersion' | 'operation' | 'id'>): string;
//# sourceMappingURL=buildPlanIdSource.d.ts.map