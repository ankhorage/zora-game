import type { ApmPlanBlocker, ApmPlanDigestPort, ApmPlanExecutorIdentity, ApmPlanResult } from '../../../types/plan.js';
import type { ApmStatusResult } from '../../../types/status.js';
/*** Revalidate a saved plan against current project evidence and executor identity before mutation. */
export declare function validateSavedPlanAsync(plan: ApmPlanResult, status: ApmStatusResult, executor: ApmPlanExecutorIdentity, digest: ApmPlanDigestPort): Promise<readonly ApmPlanBlocker[]>;
//# sourceMappingURL=validateSavedPlanAsync.d.ts.map