import type { ApmRegistryFetch } from '../../../../types/registry.js';
import type { ApmAvailabilityRequest, ApmPackageAvailabilityEvidence } from '../../../../types/status.js';
import type { ApmRegistryCacheEntry, ApmRegistryConfig } from '../../../../types/status-registry.js';
/*** Query one package with bounded cache semantics and no credential leakage into evidence. */
export declare function queryRegistryPackageAsync(input: QueryRegistryPackageInput): Promise<ApmPackageAvailabilityEvidence>;
interface QueryRegistryPackageInput {
    readonly request: ApmAvailabilityRequest;
    readonly mode: 'refresh' | 'offline';
    readonly config: ApmRegistryConfig;
    readonly fetchFn: ApmRegistryFetch;
    readonly now: () => number;
    readonly cacheTtlMs: number;
    readonly cache: Map<string, ApmRegistryCacheEntry>;
}
export {};
//# sourceMappingURL=queryRegistryPackageAsync.d.ts.map