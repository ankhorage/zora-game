import { isRecord } from '@ankhorage/utility/object';
import { isProtocolProjectPath } from '../utils/isProtocolProjectPath.js';
/*** Validate one static project ownership scope from untrusted descriptor data. */
export function isApmProjectScope(value) {
    if (!isRecord(value) || typeof value.kind !== 'string')
        return false;
    if (value.kind === 'file')
        return typeof value.path === 'string' && isProtocolProjectPath(value.path);
    if (value.kind === 'json-pointer') {
        return (typeof value.path === 'string' &&
            isProtocolProjectPath(value.path) &&
            typeof value.pointer === 'string');
    }
    return value.kind === 'dynamic' && typeof value.scope === 'string';
}
//# sourceMappingURL=isApmProjectScope.js.map