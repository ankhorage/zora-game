/*** Compare two static ownership scopes without widening file/field ownership. */
export function projectScopesEqual(left, right) {
    if (left.kind !== right.kind)
        return false;
    if (left.kind === 'dynamic')
        return right.kind === 'dynamic' && left.scope === right.scope;
    if (right.kind === 'dynamic' || left.path !== right.path)
        return false;
    if (left.kind === 'file')
        return right.kind === 'file';
    return right.kind === 'json-pointer' && left.pointer === right.pointer;
}
//# sourceMappingURL=projectScopesEqual.js.map