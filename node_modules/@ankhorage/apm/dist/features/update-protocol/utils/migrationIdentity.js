/*** Build the stable owner-scoped identity used by migration graphs and plans. */
export function migrationIdentity(owner, migrationId) {
    return `${owner}:${migrationId}`;
}
//# sourceMappingURL=migrationIdentity.js.map