import type { ApmRegistryConfig } from '../types/status-registry.js';
/*** Build one npm-compatible package metadata request using shared scoped-registry and credential rules. */
export declare function createRegistryPackageRequest(packageName: string, config: ApmRegistryConfig): {
    readonly registry: string;
    readonly url: URL;
    readonly headers: Headers;
};
//# sourceMappingURL=createRegistryPackageRequest.d.ts.map