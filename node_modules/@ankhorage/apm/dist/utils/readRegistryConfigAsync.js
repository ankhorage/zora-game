import { readFile } from 'node:fs/promises';
import path from 'node:path';
import { isMissingPathError } from '@ankhorage/utility/node/fs';
/*** Read npm-compatible user/project registry configuration without returning credential values. */
export async function readRegistryConfigAsync(input) {
    const envValues = new Map(Object.entries(input.env));
    const userValues = await readNpmrcAsync(path.join(input.home, '.npmrc'), envValues);
    const projectValues = await readNpmrcAsync(path.join(input.rootPath, '.npmrc'), envValues);
    const values = new Map([...userValues, ...projectValues]);
    const envRegistry = envValues.get('npm_config_registry') ?? envValues.get('NPM_CONFIG_REGISTRY');
    const defaultRegistry = ensureTrailingSlash(envRegistry ?? values.get('registry') ?? 'https://registry.npmjs.org/');
    const scopedRegistries = new Map([...values.entries()].flatMap(([key, value]) => /^@[^:]+:registry$/u.test(key)
        ? [[key.slice(0, key.indexOf(':')), ensureTrailingSlash(value)]]
        : []));
    return { defaultRegistry, scopedRegistries, values };
}
/*** Parse npmrc key/value lines and expand environment placeholders only at the adapter edge. */
async function readNpmrcAsync(filePath, env) {
    try {
        const text = await readFile(filePath, 'utf8');
        return new Map(text
            .split(/\r?\n/u)
            .map((line) => line.trim())
            .filter((line) => line !== '' && !line.startsWith('#') && !line.startsWith(';'))
            .flatMap((line) => parseNpmrcLine(line, env)));
    }
    catch (error) {
        if (isMissingPathError(error))
            return new Map();
        throw error;
    }
}
/*** Parse one npmrc assignment while keeping expansion local to the secret-bearing edge. */
function parseNpmrcLine(line, env) {
    const separator = line.indexOf('=');
    if (separator < 0)
        return [];
    const key = line.slice(0, separator).trim();
    const raw = line.slice(separator + 1).trim();
    const value = raw.replace(/\$\{([^}]+)\}/gu, (_match, name) => env.get(name) ?? '');
    return [[key, value]];
}
/*** Normalize registry base URLs before package URL resolution. */
function ensureTrailingSlash(value) {
    return value.endsWith('/') ? value : `${value}/`;
}
//# sourceMappingURL=readRegistryConfigAsync.js.map