import { getDevtoolsToolCommand } from './commands.js';
import { runExternalTool } from './runExternalTool.js';
export async function runStandaloneTool(toolName, argv) {
    return await runExternalTool(getDevtoolsToolCommand(toolName), argv);
}
