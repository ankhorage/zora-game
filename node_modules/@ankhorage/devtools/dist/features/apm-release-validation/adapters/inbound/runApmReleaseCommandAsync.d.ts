/*** Parse release CLI input and report the same structured validation result on every entrypoint. */
export declare function runApmReleaseCommandAsync(operation: 'sync' | 'validate', argv: readonly string[], context: CommandContext): Promise<{
    readonly exitCode: number;
}>;
interface CommandContext {
    readonly cwd: string;
    readonly writeStdout: (text: string) => void;
    readonly writeStderr: (text: string) => void;
}
export {};
