import type { ApmPackedReleaseCandidate, ApmReleaseValidationOptions, ApmReleaseValidationResult } from '../../../../types/apm-release-validation.js';
/*** Load the native public ESM export in a bounded child process only after explicit owner-code consent. */
export declare function probePackedApmExtensionAsync(input: ProbeInput): Promise<ApmReleaseValidationResult>;
interface ProbeInput {
    readonly candidate: ApmPackedReleaseCandidate;
    readonly packageRoot: string;
    readonly dependencyRoot: string;
    readonly subpath: string;
    readonly options: ApmReleaseValidationOptions;
}
export {};
