export declare const gitignoreManagedFiles: readonly [{
    readonly relativePath: ".gitignore";
    readonly render: typeof renderGitignore;
    readonly isApplicable: typeof isGitignorePresent;
}];
/*** Preserve repository-owned ignore rules while keeping documentation directories trackable. */
declare function renderGitignore(targetDirectory: string): Promise<string>;
/*** Report whether a repository already owns a `.gitignore` file that can be normalized. */
declare function isGitignorePresent(targetDirectory: string): Promise<boolean>;
export {};
