/*** Defines the Prettier files and ignore policy managed for consuming repositories. */
export declare const prettierManagedFiles: readonly [{
    readonly relativePath: "prettier.local.config.js";
    readonly render: typeof renderInitialLocalConfig;
    readonly mode: "create-only";
}, {
    readonly relativePath: ".prettierrc.js";
    readonly render: typeof renderPrettierConfig;
}, {
    readonly relativePath: ".prettierignore";
    readonly render: typeof renderPrettierIgnore;
}];
/*** Renders the shared Prettier wrapper for the target package module type. */
declare function renderPrettierConfig(targetDirectory: string): Promise<string>;
/*** Preserves an existing repository Prettier config as its initial local extension. */
declare function renderInitialLocalConfig(targetDirectory: string): Promise<string>;
/*** Preserves repository-owned ignore entries while appending required generated-artifact ignores. */
declare function renderPrettierIgnore(targetDirectory: string): Promise<string>;
export {};
