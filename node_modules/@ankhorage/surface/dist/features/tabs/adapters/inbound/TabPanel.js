import React from 'react';
import { View } from 'react-native';
import { useTabsContext } from '../../composition/useTabsContext';
const TAB_PANEL_ROLE = 'tabpanel';
/*** Renders the content panel associated with the active tab value. */
export function TabPanel({ value, children, testID }) {
    const { activeValue, getPanelId, getTabId } = useTabsContext();
    if (activeValue !== value)
        return null;
    return (<View accessibilityLabelledBy={getTabId(value)} accessibilityRole={TAB_PANEL_ROLE} nativeID={getPanelId(value)} testID={testID}>
      {children}
    </View>);
}
//# sourceMappingURL=TabPanel.js.map